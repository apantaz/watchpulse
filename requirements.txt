# Phase 1: ingestion only. api/ and warehouse/ dependencies (fastapi, dbt-duckdb, ...)
# are added in later phases per docs/architecture.md #25.
httpx>=0.27
tenacity>=8.2
pyarrow>=16.0
python-dotenv>=1.0
pytest>=8.0
