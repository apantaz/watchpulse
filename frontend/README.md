# Frontend

Planned deterministic discovery UI for `v0.5`.

The framework will be selected through an ADR. This component calls only the
WatchPulse API and may persist guest preferences in browser-local storage.
